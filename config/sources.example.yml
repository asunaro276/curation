# Tech News Sources Configuration
# Copy this file to config/sources.yml and customize as needed

sources:
  # RSS Feed Example
  - type: rss
    name: "Hacker News"
    url: "https://news.ycombinator.com/rss"
    enabled: true

  # GitHub Trending Example (All languages)
  - type: github_trending
    name: "GitHub Trending"
    enabled: true

  # GitHub Trending Example (Specific language)
  - type: github_trending
    name: "GitHub Trending Ruby"
    language: "ruby"
    enabled: true

  # Additional RSS Feed Examples
  - type: rss
    name: "Ruby Weekly"
    url: "https://rubyweekly.com/rss"
    enabled: false

  - type: rss
    name: "JavaScript Weekly"
    url: "https://javascriptweekly.com/rss"
    enabled: false

  - type: rss
    name: "Dev.to - Ruby"
    url: "https://dev.to/feed/tag/ruby"
    enabled: false

# Limits and constraints
limits:
  # Maximum articles to fetch per source (to control API costs)
  max_articles_per_source: 5

  # Maximum content tokens for Claude API (to control costs)
  # Rough estimate: 1 token ≈ 3-4 characters for Japanese
  max_content_tokens: 4000

  # API request timeout (seconds)
  api_timeout: 30

  # HTTP connection timeout (seconds)
  http_timeout: 10

# Slack posting configuration
slack:
  # Interval between posts (seconds) - to avoid rate limiting
  post_interval: 1

  # Maximum posts per batch
  max_posts_per_batch: 10

# Summarization configuration
summarization:
  # Custom prompts for article summarization
  # カスタムプロンプトで要約スタイルを完全にカスタマイズ可能

  # システムプロンプト：要約者の役割を定義
  system_prompt: |
    あなたは技術ニュースのキュレーターです。
    以下の記事を日本語で要約してください。

  # 出力フォーマット：要約の形式を指定
  output_format: |
    以下の形式で出力してください:
    - 2-3文の簡潔な要約
    - 重要なポイント（箇条書き、最大3点）

# ------------------------------------------------------------------------------
# 既存テンプレート（旧版）からの移行ガイド
# 以下は旧バージョンで利用可能だった5つの事前定義テンプレートのYAML例です。
# 必要に応じてコピー&ペーストして使用してください。
# ------------------------------------------------------------------------------

# 【1. Default テンプレート】標準形式（2-3文の要約 + 最大3点の箇条書き）
# summarization:
#   system_prompt: |
#     あなたは技術ニュースのキュレーターです。
#     以下の記事を日本語で要約してください。
#   output_format: |
#     以下の形式で出力してください:
#     - 2-3文の簡潔な要約
#     - 重要なポイント（箇条書き、最大3点）

# 【2. Concise テンプレート】超簡潔版（1-2文のみ）
# summarization:
#   system_prompt: |
#     あなたは技術ニュースのキュレーターです。
#     以下の記事を極めて簡潔に日本語で要約してください。
#   output_format: |
#     以下の形式で出力してください:
#     - 1-2文の超簡潔な要約

# 【3. Detailed テンプレート】詳細版（4-5文の要約 + 5点程度の箇条書き）
# summarization:
#   system_prompt: |
#     あなたは技術ニュースのキュレーターです。
#     以下の記事を詳細に日本語で要約してください。
#   output_format: |
#     以下の形式で出力してください:
#     1. 4-5文の詳細な要約
#     2. 重要なポイント（箇条書き、5点程度）

# 【4. Technical テンプレート】技術特化版（アーキテクチャ、技術スタック、パフォーマンスなどに焦点）
# summarization:
#   system_prompt: |
#     あなたは技術ニュースのキュレーターです。
#     以下の記事を技術的な観点から日本語で要約してください。
#   output_format: |
#     以下の形式で出力してください:
#     1. 2-3文の技術的な要約
#     2. 技術的な詳細（箇条書き、最大5点）
#        - アーキテクチャの特徴
#        - 使用技術・技術スタック
#        - パフォーマンスへの影響
#        - セキュリティへの配慮
#        - その他の技術的なポイント

# 【5. Bullet Points テンプレート】箇条書きのみ（5-7点）
# summarization:
#   system_prompt: |
#     あなたは技術ニュースのキュレーターです。
#     以下の記事を箇条書き形式で日本語で要約してください。
#   output_format: |
#     以下の形式で出力してください:
#     - 箇条書きのみ（5-7点）
#     - 各項目は簡潔に1文で記述

# ------------------------------------------------------------------------------
# Tips:
# - Start with a small number of sources and max_articles_per_source to control costs
# - Enable sources one at a time to verify they work correctly
# - Adjust max_content_tokens based on your Anthropic API usage limits
# - The cron schedule in .github/workflows/run-curation.yml controls execution frequency
